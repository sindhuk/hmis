</td></td>
<table align="center">
<% form_for(@bedmaster) do |f| %>
  <%= f.error_messages %>

  <tr>
    <td><%= f.label :roommaster_id %></td>
    <td><%= f.text_field :roommaster_id %></td>
  </tr>
  <tr>
    <td><%= f.label :bed_name %></td>
    <td><%= f.text_field :bed_name %></td>
  </tr>
  <tr>
    <td><%= f.label :status %></td>
    <td><%= f.text_field :status %></td>
  </tr>
  <tr>
    <td align="center" colspan="2"><%= f.submit 'Update' %></td>
  </tr>
<% end %>
</table>
<%= link_to 'Show', bedmasters_path %>