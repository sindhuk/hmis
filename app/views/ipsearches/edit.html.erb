<h1>Editing ipsearch</h1>

<% form_for(@ipsearch) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @ipsearch %> |
<%= link_to 'Back', ipsearches_path %>